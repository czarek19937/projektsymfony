{% extends "FilmyBundle::bars.html.twig" %}

	{% block header %}
		<h1>ordersbuy</h1>
	{% endblock %}



	{% block content %}
		
		
			
		


            {% set z = 'Zapłacone'%}
            {{z}}
			{% set i=0%}
    		{%for orders in orders if orders.status==z %}
    		
    		{% set i = i + 1 %}
    		<br>Zamówienie {{i}}
    		<br>Status zamówienia:{{ orders.status}}
    		<br>Forma zapłaty:{{ orders.form}}
    		<br>Warunki zamówienia:{{ orders.conditions}}
    		<br>Film wypożyczony:
    		{% for movies in movies %}
                        {% if (orders.idfilm == movies.id) %}
                        {{ movies.title }}<br>
                            {% endif %}
                            {% endfor %}
    		{%endfor%}
    		
    		
 
    
    <input type=button onClick="alert('wyświetla się komunikat')" value="Ogladaj">

    
   
		
			

		
		
    
        
 
    	
 

	{% endblock %}